deps:
    - remove revise from deps
    - remove test form buildstep
    - git upstream
    - build scenarios
    - compile camnas on arm (grace)
    - podman build issues

    - c test library linking

Warnings:
    Vscode cmnd+enter adds random execution time overhead
     - use: CUDA_VISIBLE_DEVICES=0 julia -t4,0 test/test_interface.jl

    - Runtime switch calculates gpu and cpu decomp
    - Runtime switch enabled gived bad benchmarks, performance problems

Notes:
    - CUDSS takes very long on first solve
    
    - Memory gets eaten somewhere. GC between solves or after decomp causes it?

    - fix julia version:
        juliaup add 1.12.1
        juliaup default 1.12.1
    

    - build
        docker run -it -v $(pwd):/dpsim sogno/dpsim:dev bash